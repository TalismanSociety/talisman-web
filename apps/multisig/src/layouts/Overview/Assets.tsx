import { css } from '@emotion/css'

const Assets = () => {
  return (
    <section
      className={css`
        grid-area: assets;
        background-color: green;
      `}
    >
      assets
    </section>
  )
}

export default Assets
