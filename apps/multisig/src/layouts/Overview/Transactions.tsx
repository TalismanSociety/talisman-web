import { css } from '@emotion/css'

const Transactions = () => {
  return (
    <section
      className={css`
        grid-area: transactions;
        background-color: green;
      `}
    >
      transactions
    </section>
  )
}

export default Transactions
