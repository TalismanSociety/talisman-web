import { css } from '@emotion/css'

const Footer = () => {
  return (
    <footer
      className={css`
        grid-area: footer;
        background-color: green;
      `}
    >
      footer
    </footer>
  )
}

export default Footer
