import { css } from '@emotion/css'

const Footer = () => {
  return (
    <footer
      className={css`
        margin-top: 176px;
        display: grid;
        background-color: red;
      `}
    >
      footer
    </footer>
  )
}

export default Footer
