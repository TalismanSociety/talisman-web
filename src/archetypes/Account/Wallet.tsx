import styled from 'styled-components'

const Wallet = styled(
  ({
    id,
    className
  }) => {
    return <div
      className={`account-wallet ${className}`}
      >
      todo
    </div>
  })
  `
  `

export default Wallet