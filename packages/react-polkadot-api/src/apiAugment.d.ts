// NOTE: this is purely so `AugmentedQueries` doesn't get compiled to `never` when building
// eslint-disable-next-line @typescript-eslint/triple-slash-reference
/// <reference types="@polkadot/api-augment/substrate" />
